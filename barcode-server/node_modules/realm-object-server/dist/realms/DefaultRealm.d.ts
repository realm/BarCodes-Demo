import { RealmDefinition } from "../RealmFactory";
import * as Realm from 'realm';
export declare class DefaultRealmVersion {
    static schema: Realm.ObjectSchema;
    version: number;
    static CurrentVersion: number;
    static SingletonId: number;
}
export declare function initialize(realm: Realm): void;
export declare const Definition: RealmDefinition;
