import { StatsStorage } from "../stats";
export interface MixpanelEmitterConfiguration {
    serverVersion: string;
}
export declare class MixpanelService {
    statsStorage: StatsStorage;
    private config;
    private adminRealm;
    private interval;
    private timeout;
    constructor(config: MixpanelEmitterConfiguration);
    private start(server);
    private stop();
    private sendUsage();
}
