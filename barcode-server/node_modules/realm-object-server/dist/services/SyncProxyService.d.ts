import { StatsSink } from "../stats/StatsSink";
export declare class SyncProxyService {
    stats: StatsSink;
    private logger;
    private server;
    private backendPromises;
    private activeConnectionsGauge;
    private connectionDurationHistogram;
    private start(server);
    private stop();
    private setLogger(logger);
    private syncMasterChanged(backend, handle);
    private closeBackend(backend);
    private parseTokenAndGetLabel(req, socket, head);
    private nextSocketID;
    private websocketHandler(req, socket, head);
    private getBackend(syncLabel);
}
