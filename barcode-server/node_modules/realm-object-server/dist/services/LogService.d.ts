export interface ILogEntry {
    level: string;
    message: string;
    context: {
        [key: string]: string;
    };
}
export declare class LogService {
    private buffers;
    private logger;
    private rootLogger;
    private listener;
    private wss;
    constructor();
    private appendTestEntries();
    private streamLogs(req, socket, head);
    private setRootLogger(l);
    private setLogger(l);
    private message(level, msg, ctx);
    private isRelevantLevel(actualLevel, targetLevel);
    private start(server);
    private stop();
}
