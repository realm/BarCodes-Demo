"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
function readTemplate(name, format) {
    return fs.readFileSync(path.resolve(__dirname, `${name}.${format}`), 'utf8');
}
exports.emails = {
    confirmEmail: {
        text: readTemplate('confirm-email', 'txt'),
        html: readTemplate('confirm-email', 'html'),
    },
    resetPassword: {
        text: readTemplate('reset-password', 'txt'),
        html: readTemplate('reset-password', 'html'),
    }
};
//# sourceMappingURL=index.js.map