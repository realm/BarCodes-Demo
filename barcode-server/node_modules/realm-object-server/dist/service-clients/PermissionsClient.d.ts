import { ServiceClientBase } from "./ServiceClientBase";
import { IPermissionChangeRequest } from '../services/PermissionsService';
export declare class PermissionsClient extends ServiceClientBase {
    protected serviceName: string;
    protected baseRoute: string;
    onDemand(token: string): Promise<{
        userId: string;
        affectedPermissionsCount: number;
    }>;
    applyPermissions(changeRequest: IPermissionChangeRequest): Promise<{
        affectedUsers: number;
    }>;
}
