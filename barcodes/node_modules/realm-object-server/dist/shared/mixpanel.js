"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const Mixpanel = require("mixpanel");
const util_1 = require("./util");
let realMixPanel;
let distinctId;
exports.mixpanel = {
    identify: (id) => {
        distinctId = id;
        realMixPanel = Mixpanel.init('953cbc7bc5e8daa02b91425ac92d36dd');
    },
    track: (event, payload) => __awaiter(this, void 0, void 0, function* () {
        if (realMixPanel) {
            const newPayload = Object.assign(payload, { "$distinct_id": distinctId });
            return util_1.Promisify(realMixPanel.track(event, newPayload));
        }
    })
};
//# sourceMappingURL=mixpanel.js.map