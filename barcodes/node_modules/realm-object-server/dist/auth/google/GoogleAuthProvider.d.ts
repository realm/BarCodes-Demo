import { User } from "../../realms/AdminRealm";
import { AuthProvider } from "../AuthProvider";
export interface GoogleAuthProviderConfig {
    clientId: string;
}
export declare class GoogleAuthProvider extends AuthProvider {
    name: string;
    private readonly clientId;
    constructor(config: GoogleAuthProviderConfig);
    authenticateOrCreateUser(body: any): Promise<User>;
}
